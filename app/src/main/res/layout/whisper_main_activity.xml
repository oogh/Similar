<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".whisper.WhisperMainActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/tb_whisper_main_activity"
        android:layout_width="match_parent"
        android:layout_height="?android:attr/actionBarSize"
        android:background="@color/colorSystemUI"
        android:elevation="4dp"
        app:titleTextColor="@color/colorTitleText">

        <TextView
            android:id="@+id/tv_whisper_main_activity_title"
            style="@style/TextAppearance.AppCompat.Widget.ActionBar.Title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:drawablePadding="4dp"
            android:text="心愿单"
            android:textColor="@android:color/white" />
    </android.support.v7.widget.Toolbar>

    <TextView
        android:id="@+id/tv_empty_whisper_main"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:fontFamily="@font/roboto_light"
        android:text="你有什么未了的心语么"
        android:textSize="20sp"
        android:visibility="visible" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rv_whisper_main_activity"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/btn_whisper_main_activity"
        android:layout_below="@+id/tb_whisper_main_activity"
        android:visibility="gone" />

    <Button
        android:id="@+id/btn_whisper_main_activity"
        android:layout_width="match_parent"
        android:layout_height="?android:attr/actionBarSize"
        android:layout_alignParentBottom="true"
        android:background="@color/colorShadow"
        android:fontFamily="@font/roboto_bold"
        android:text="飞向心语的银河"
        android:textColor="@color/colorWhite"
        android:textSize="16sp" />


</RelativeLayout>